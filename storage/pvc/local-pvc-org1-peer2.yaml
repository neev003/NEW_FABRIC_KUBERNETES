apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-pvc-org1-peer2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-storage
  volumeName: local-pv-org1-peer2
